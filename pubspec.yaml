name: x_router
description: A new Flutter package project.
version: 0.0.1
author: cedvdb
homepage:
publish_to: none

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=2.0.0"

dependencies:
  flutter:
    sdk: flutter
  route_parser:
    git:
      url: https://github.com/cedvdb/route_parser.git

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
