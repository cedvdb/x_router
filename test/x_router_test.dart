import 'package:flutter_test/flutter_test.dart';

void main() {
  group('XRouter', () {
    group('goTo', () {});
    group('replace', () {});

    group('pop', () {});

    group('back', () {});

    group('redirects', () {});
  });
}
